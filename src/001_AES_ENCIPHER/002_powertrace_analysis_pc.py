import encipher_api, scope_api, pickle, base64
import numpy as np
from cryptography.hazmat.backends import default_backend
from cryptography.hazmat.primitives.ciphers import Cipher, algorithms, modes
"""
Perform Differential Power Analysis to disclose the secret key based on the power traces you recorded, as discussed in the lecture.
Do not forget to annotate your implementation with comments and describe what you have been doing in the lab report.
Plots depicting your analysis must be included in the Lab report.
"""
def extractData(path):
    data = []
    with open(path, 'rb') as f:
        data = pickle.load(f)
        test = (data[0][0], data[0][2])
        filtered_data = [(cleartext, traces) for cleartext, traces, _ in data if traces]
        cleartext, traces = zip(*filtered_data)
        return cleartext, np.array(traces), test
    
def hamming_weight(n):
    weight = 0
    while n:
        weight += n & 1
        n >>= 1
    return weight

def perform_DPA(cleartext, traces):
    sbox = [
        
    ]

    n_hypotheses  = 256
    vhamming_weight = np.vectorize(hamming_weight)
    
    
    result = []
    for byte_index in range(16):
        print(f"\nIteration {byte_index + 1} on byte_index")
        max_total = 0
        index = -1
        for hypothesis in range(n_hypotheses):
            print(f"\r Iteration {hypothesis}/255 on hypothesis", end = "")
            cleartext_ints = np.array([ct[byte_index] for ct in cleartext], dtype=np.uint8)

            intermediate = np.array([sbox[ct ^ hypothesis] for ct in cleartext_ints])

            hw = vhamming_weight(intermediate)
            
            zeros = np.zeros(120000)
            ones = np.zeros(120000)
            counterZ = 0
            counterO = 0
            
            for i, weight in enumerate(hw):
                if weight % 2 == 0:
                    counterZ += 1
                    zeros += traces[i]
                else:
                    counterO += 1
                    ones += traces[i]
            
            zeros_average = zeros / counterZ
            ones_average = ones /counterO
            
            max_value = max(abs(zeros_average) - abs(ones_average))
            
            if max_value > max_total:
                index = hypothesis
                max_total = max_value
        if index == -1:
            raise Exception
        result.append(index)
    return result

def encrypt_text(text, key):
    # Pad the text to be a multiple of 16 bytes (block size for AES)
    padded_text = text + (16 - len(text) % 16) * b' '

    cipher = Cipher(algorithms.AES(key), modes.ECB(), backend=default_backend())
    encryptor = cipher.encryptor()

    ciphertext = encryptor.update(padded_text) + encryptor.finalize()
    return base64.b64encode(ciphertext)

def main():
    cleartext, traces, test = extractData('combined_results.pkl')
    key = perform_DPA(cleartext, traces)
    
    # Convertir la lista de enteros a bytes
    key_bytes = bytes(key)
    
    print("\nClave hipotética: ", key_bytes)
    
    # Cifrar el texto de prueba con la clave hipotética
    encrypted_text = encrypt_text(test[0], key_bytes)
    
    # Comparar el resultado cifrado con el texto cifrado de prueba
    if encrypted_text == test[1]:
        print("Clave correcta.")
    else:
        print("Clave incorrecta.")

if __name__ == '__main__':
    main()
